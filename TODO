* Display cause of error when a make fails.
* Detect errors opening log file.
* Add these changes to the docs.
* Add a sort of logging subroutine so that verbosity can be controlled.
* Don't delete the entire build directory if we're using checkout-only and
  cleaning the build dir.
* Update the documentation included in the tarball.
* Port kdesvn-pywizard (or write a Ruby/Korundum replacement).
