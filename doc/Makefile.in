# Makefile.in generated by unsermake
####################################

top_builddir = ../../..
top_srcdir = @top_srcdir@
srcdir = @srcdir@
.FORWARDS: all install install-data install-exec clean check force-reedit

all:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	unsermake all

install:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	unsermake install

install-data:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	unsermake install-data

install-exec:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	unsermake install-exec

clean:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	unsermake clean

check:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	unsermake check

force-reedit:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	unsermake force-reedit

$(DESTDIR)$(kde_htmldir)/en/kdesvn-build/common: $(UNSERMAKE_FORCE_INSTALL)
	-rm -f $(DESTDIR)$(kde_htmldir)/en/kdesvn-build/common
	$(LN_S) $(kde_libs_htmldir)/en/common $(DESTDIR)$(kde_htmldir)/en/kdesvn-build/common

$(DESTDIR)$(kde_htmldir)/en/kdesvn-build/index.cache.bz2: $(top_builddir)/doc/scripts/kdesvn-build/index.cache.bz2 $(UNSERMAKE_FORCE_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(kde_htmldir)/en/kdesvn-build
	@if test -f $(top_builddir)/doc/scripts/kdesvn-build/index.cache.bz2; then \
	  echo $(INSTALL_DATA) $(top_builddir)/doc/scripts/kdesvn-build/index.cache.bz2 $(DESTDIR)$(kde_htmldir)/en/kdesvn-build/ ;\
	  $(INSTALL_DATA) $(top_builddir)/doc/scripts/kdesvn-build/index.cache.bz2 $(DESTDIR)$(kde_htmldir)/en/kdesvn-build/; \
	fi

$(DESTDIR)$(kde_htmldir)/en/kdesvn-build/index.docbook: $(top_srcdir)/doc/scripts/kdesvn-build/index.docbook $(UNSERMAKE_FORCE_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(kde_htmldir)/en/kdesvn-build
	@echo $(INSTALL_DATA) $(top_srcdir)/doc/scripts/kdesvn-build/index.docbook $(DESTDIR)$(kde_htmldir)/en/kdesvn-build/index.docbook ;\
	$(INSTALL_DATA) $(top_srcdir)/doc/scripts/kdesvn-build/index.docbook $(DESTDIR)$(kde_htmldir)/en/kdesvn-build/index.docbook

$(top_builddir)/doc/scripts/kdesvn-build/Makefile: $(top_builddir)/config.status $(top_srcdir)/doc/scripts/kdesvn-build/Makefile.in
	cd $(top_builddir) && $(SHELL) ./config.status doc/scripts/kdesvn-build/Makefile

$(top_builddir)/doc/scripts/kdesvn-build/index.cache.bz2: $(KDE_XSL_STYLESHEET) $(top_srcdir)/doc/scripts/kdesvn-build/index.docbook
	@if test -n "$(MEINPROC)"; then echo $(MEINPROC) --check --cache $(top_builddir)/doc/scripts/kdesvn-build/index.cache.bz2 $(top_srcdir)/doc/scripts/kdesvn-build/index.docbook; $(MEINPROC) --check --cache $(top_builddir)/doc/scripts/kdesvn-build/index.cache.bz2 $(top_srcdir)/doc/scripts/kdesvn-build/index.docbook; fi

all: $(top_builddir)/doc/scripts/kdesvn-build/Makefile
clean-docs-top_doc_scripts_kdesvn_build: 
	-rm -f $(top_builddir)/doc/scripts/kdesvn-build/index.cache.bz2

docs-top_doc_scripts_kdesvn_build: $(top_builddir)/doc/scripts/kdesvn-build/index.cache.bz2
final: 
	@echo no programs available to recreate as final. Call in subdirs

install-docs-top_doc_scripts_kdesvn_build: $(DESTDIR)$(kde_htmldir)/en/kdesvn-build/common $(DESTDIR)$(kde_htmldir)/en/kdesvn-build/index.cache.bz2
install-nls-top_doc_scripts_kdesvn_build: $(DESTDIR)$(kde_htmldir)/en/kdesvn-build/index.docbook
uninstall-docs-top_doc_scripts_kdesvn_build: 
	-rm -rf $(kde_htmldir)/en/kdesvn-build

uninstall-nls-top_doc_scripts_kdesvn_build: 
	for base in index.docbook; do \
	  rm -f $(DESTDIR)$(kde_htmldir)/en/kdesvn-build/$$base ;\
	done

.PHONY: all clean-docs-top_doc_scripts_kdesvn_build docs-top_doc_scripts_kdesvn_build final install-docs-top_doc_scripts_kdesvn_build uninstall-docs-top_doc_scripts_kdesvn_build uninstall-nls-top_doc_scripts_kdesvn_build
